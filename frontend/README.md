# Narrative Forge Frontend

React + TypeScript + Vite frontend for 文脉工坊（Narrative Forge）.

## Development

```bash
npm install
npm run dev
```

The development server proxies `/api` requests to the backend configured in `vite.config.ts`.

## Build

```bash
npm run build
```
